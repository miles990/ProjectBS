OnDamageTaken
{
        BeginIf(Self.HP <= 0);
        SetStatus(Self, HP, 1);
        GainBuff(Self, 25, 3);
        RemoveBuff(Self. 24);
        EndIf();
}

OnActived
{
        BeginIf(Self.HP > 0);
        AddStatus(HP, Self.MaxHP*0.2);
        EndIf();
}